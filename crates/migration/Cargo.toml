[package]
name         = "migration"
version.workspace = true
edition.workspace = true
publish      = false

[lib]
name = "migration"
path = "src/lib.rs"

[dependencies]
tokio.workspace = true

[dependencies.sea-orm-migration]
features = [
    "sqlx-sqlite",
    "runtime-tokio",
]
version = "2.0.2"
